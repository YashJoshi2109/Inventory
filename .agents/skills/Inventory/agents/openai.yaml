interface:
  display_name: "Inventory"
  short_description: "Repo-specific patterns and workflows for Inventory"
  default_prompt: "Use the Inventory repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true